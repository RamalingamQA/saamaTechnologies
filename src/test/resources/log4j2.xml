<?xml version="1.0" encoding="UTF-8"?>

<Configuration status="WARN">

    <Appenders>
        <!-- Console Appender -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{HH:mm:ss.SSS} {%t} %-5level - %msg%n"/>
        </Console>

        <!-- File Appender -->
        <File name="File" fileName="E:\\D drive\\Saama\\selenium\\logs\\log4j.log" append="true">
            <PatternLayout pattern="%d{HH:mm:ss.SSS} {%t} %-5level - %msg%n"/>
        </File>
    </Appenders>

    <Loggers>
        <!-- Custom Logger for Selenium -->
        <Logger name="selenium" level="debug" additivity="true">
            <AppenderRef ref="Console"/>
        </Logger>

        <!-- Root Logger -->
        <Root level="debug">
            <AppenderRef ref="File"/>
        </Root>
    </Loggers>
</Configuration>
